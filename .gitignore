*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.idea/
# Gemma model files — never commit (multi-GB)
*.litertlm
*.task
/models/

# Claude Code session state — ignore everything except the team-shared settings.json
/.claude/*
!/.claude/settings.json

# Python (emulator)
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Review screenshots captured during design polish sessions
/.screens/

# Agent-session caches (HyperFrames skills cached by the design-skill agents)
.agents/
skills-lock.json

# Impeccable design-skill brief — session-local working doc
.impeccable.md

# HyperFrames demo-video sub-project — stays on disk locally for the
# primary dev's reference, but the Android repo doesn't track it
carcopilot-demo/

# On-device smoke-test logcat captures — per-run artifacts, not source
smoke-*.log
*-dump.log

# Misc runtime log captures saved while debugging the app
full_logcat.txt
car_logs.txt
crash_buffer.txt
*.logcat

# Build artifacts that should never land in git
/app/build/
/app/release/
*.apk
*.aab

# OS / editor cruft
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# Demo video footage captured for the hackathon submission
submission/footage/
